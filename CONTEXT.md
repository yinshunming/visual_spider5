# 可视化网页采集

本项目帮助具备基础网页与数据概念的人员，通过可视化操作定义并执行网页数据采集。

## Language

**采集人员**：
懂基本网页结构和数据概念，通过可视化操作配置网页数据采集的数据运营或分析人员。
_Avoid_: 普通用户、爬虫开发者

**列表采集**：
从包含重复数据项的网页开始批量提取数据，可继续访问各数据项对应的内容页补充字段。
_Avoid_: 列表抓取

**单页采集**：
从一个具体网页直接提取字段，不依赖重复列表、翻页或向其他页面导航。
_Avoid_: 详情页采集、单页抓取

**内容页**：
列表项所链接的具体网页，在列表采集中用于补充该条数据的字段。
_Avoid_: 详情页、单页

**采集任务**：
可保存并重复使用的网页数据采集定义，包含入口、采集模式、字段及页面导航规则。
_Avoid_: 爬虫项目、采集运行、作业

**采集运行**：
采集任务的一次实际执行，拥有独立的状态、日志、结果与失败信息。
_Avoid_: 采集任务、作业实例

**任务快照**：
采集运行开始时固化的采集任务定义，保证该次运行不受后续任务编辑影响。
_Avoid_: 任务版本、当前任务

**采集字段**：
采集人员为结果数据定义的命名值，其内容来自网页文本、元素属性或页面来源信息。
_Avoid_: 抓取项、数据列

**元素定位规则**：
用于在网页中识别列表项、采集字段、内容页入口或翻页操作所对应元素的规则。
_Avoid_: 抓取规则、定位器

**结果记录**：
一次采集运行产生的一条扁平数据；列表采集中的一个列表项或单页采集中的整个页面各对应一条结果记录。
_Avoid_: 数据行、嵌套结果

**运行事件**：
采集运行过程中产生的、对采集人员可见的结构化事件，记录级别、执行阶段、相关 URL、错误码与摘要消息；与仅供运维排查的技术日志不同，运行事件不包含完整堆栈或页面内容。一个 [[采集运行]] 关联一串运行事件，用于运行详情页展示和事后解释。
_Avoid_: 运行日志、技术日志

**停止原因**：
采集运行停止时记录的明确原因（如正常完成、用户取消、入口失败、重试耗尽、达到页数/记录数/时长上限、遇 429/403/验证码、应用中断）；与 [[采集运行]] 的终态配套，出现在运行详情页，便于采集人员判断结果是完整的还是被限制中断的。
_Avoid_: 失败原因、退出码

**运行状态**：
采集运行在其生命周期中所处的阶段与终态：等待中、运行中，以及终态成功、部分成功、失败、已取消、已中断。其中**部分成功**指运行既写入了结果记录又有失败（[[停止原因]] 记录首个失败原因），区别于全成功与全失败；列表采集因每个列表项独立成败而首次可能产生部分成功。采集人员据此判断结果是否完整可用。与 [[采集运行]] 配套。
_Avoid_: 任务状态、运行结果

**配置会话**：
采集人员为配置某个采集任务而打开的交互式远程浏览器会话；在隔离的 BrowserContext 中浏览目标页面、点选元素、定义字段与定位规则，结束后不保留 Cookie、缓存或页面交互状态。一名用户同时最多 1 个，无操作 15 分钟或满 2 小时自动关闭。区别于 [[采集运行]]：配置会话是人驱动的配置过程，不产生结果记录；采集运行是按任务快照自动执行、产生结果。
_Avoid_: 浏览器会话、远程会话

**浏览模式/选择模式**：
配置会话中远程页面的两种交互模式。浏览模式允许点击、输入、滚动、前进、后退和刷新，用于到达目标页面；选择模式下点击只选中 DOM 元素、不触发原页面动作，用于定义列表项、采集字段、内容页入口和翻页操作对应的元素定位规则（见 [[元素定位规则]]）。
_Avoid_: 编辑模式、拾取模式

**下一页元素**：
[[列表采集]] 中负责触发“切换到下一页列表”的 DOM 元素；被点选后产生新的列表页 URL，与 [[元素定位规则]] 关联。在 M5 的 `PaginationRule.mode=NEXT_PAGE` 路径下使用；重复页保护（URL + 列表项内容哈希双重比对）与该元素配合生效。
_Avoid_: 翻页按钮、分页器、页码链接

**加载更多元素**：
[[列表采集]] 中负责在当前列表页追加新列表项的 DOM 元素；被点选后不改变当前 URL，只增加当前页列表项数量，与 [[元素定位规则]] 关联。在 M5 的 `PaginationRule.mode=LOAD_MORE` 路径下使用；连续点击 1 秒后列表项数量未增长则触发 `STOP_PAGINATION_NO_NEW_ITEMS`。
_Avoid_: 展开按钮、无限滚动、查看更多

**列表页字段**：
从列表项元素内部直接抽取的 [[采集字段]]，与列表项 DOM 上下文绑定（见 [[元素定位规则]]）；在 [[结果记录]] 中以字段名出现一次。在 M5 的 `FieldDefinition.scope=LIST` 路径下使用，与 [[内容页字段]] 同名时按字段名合并到同一记录。
_Avoid_: 主字段、当前页字段

**内容页字段**：
从 [[内容页]] DOM 中抽取的 [[采集字段]]，与列表项字段同名时按字段名合并到同一 [[结果记录]]。区别于 [[列表页字段]]：内容页字段的内容来自一层内容页，且只在 [[列表采集]] 中存在；[[单页采集]] 不引入此区分。在 M5 的 `FieldDefinition.scope=CONTENT` 路径下使用；内容页 navigate 失败时该字段最终为 null，但对应 record 仍按 [[列表页字段]] 写入 `record_count_final`，并计入 `content_fail_count`。
_Avoid_: 二级字段、详情字段、补充字段
