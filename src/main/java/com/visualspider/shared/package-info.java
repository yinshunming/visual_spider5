/**
 * shared 模块。
 *
 * <p>本包承载跨模块共享的稳定标识符、时钟与小型错误类型。
 * 不放业务规则、不放实现细节、不放可变全局状态。
 *
 * <p>跨里程碑使用（M1 起逐步沉淀；M0.5 仅完成包结构占位）。
 * 按 AGENTS "shared 只放稳定标识符、时钟和小型错误类型"约束维护。
 *
 * <p>本包遵守 ADR-0003（按业务模块组织深模块）：
 * 仅通过稳定类型对外暴露能力；不得反向依赖任何业务模块。
 */
package com.visualspider.shared;