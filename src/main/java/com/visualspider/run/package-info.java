/**
 * run 模块。
 *
 * <p>本包承载 run 业务能力：任务快照固化、运行状态机（等待/运行中/终态）、
 * 专用运行 lane、用户取消与有界调度、全局容量限制与单实例运行约束。
 *
 * <p>M3 启用；M0.5 仅完成包结构占位。
 *
 * <p>本包遵守 ADR-0003（按业务模块组织深模块）：
 * 仅通过稳定 interface 对外暴露能力；隐藏 PostgreSQL 权威状态、JVM 有界执行器、
 * lane 线程亲和性、应用启动时的中断标记逻辑。
 */
package com.visualspider.run;